<div id="widget_bmi" class="col-md-3">
    <div class="card mb-4 shadow-sm">
        <div class="card-body">
            <% if bmi %>
            
            <h3 class="card-title">
                B.M.I:
            </h3>

            <h2 class=""><%= bmi %></h2>
            
            <% else %>
            
            <small>You don't have enough data to calculate your BMI</small> 
            
            <% end %>
        </div>
    </div>
</div>
