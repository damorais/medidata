<% content_for :titlebar do %>
  <h2 style="color:white">HDL Cholesterol</h2>
<% end %>

<% if flash[:success] %>
  <div class="alert alert-success" role="alert">
    <%= flash[:success] %>
  </div>
<% end %>

<table class="table table-striped">
  <tr>
    <th scope="col">ID</th>
    <th scope="col">Value</th>
    <th scope="col">Date</th>
    <th scope="col"></th>
  </tr>
 
  <% @hdls.each do |hdl| %>
    <tr>
      <td><%= hdl.id %></td>
      <td><%= hdl.value %></td>
      <td><%= hdl.date %></td>
      <td>
        <%= link_to "Edit", edit_profile_hdl_path(profile_email: @profile.email, id: hdl.id), class: "btn btn-sm btn-primary" %> |  
        <%= link_to "Delete", profile_hdl_path(profile_email: @profile.email, id: hdl.id), 
            method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-sm btn-danger" %> 
        </td>
    </tr>
  <% end %>
  
</table>
<%= link_to 'Add HDL Cholesterol', new_profile_hdl_path(profile_email: @profile.email),  class: "btn btn-sm btn-success"  %>