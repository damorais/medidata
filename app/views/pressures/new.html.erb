<h2>Pressão Sanguínea</h2>

<% if flash[:success] %>
      <div class="alert alert-success" role="alert">
	    <%= flash[:success] %>
      </div>
<% end %>

<%= form_with scope: :pressure, url: profile_pressures_path, local: true do |form| %>

<div class="form-group">
    <%= form.label :sis %>
  <% if @pressure and @pressure.errors.messages[:sis] %>
  <div class="text-danger small">
      <%= @pressure.errors.messages[:sis].to_sentence %>
  </div>
<% end %>
  <%= form.number_field :sis, class: "col-sm-1" %>
</div>

<div class="form-group">
    <%= form.label :dia %>
    <% if @pressure and @pressure.errors.messages[:dia] %>
      <div class="text-danger small">
          <%= @pressure.errors.messages[:dia].to_sentence %>
      </div>
    <% end %>
    <%= form.number_field :dia, class: "col-sm-1"  %>
</div>

<div class="form-group">
    <%= form.label :data %>
    <% if @pressure and @pressure.errors.messages[:data] %>
    <div class="text-danger small">
        <%= @pressure.errors.messages[:data].to_sentence %>
    </div>
    <% end %>
    <%= form.datetime_select :data, class: "form-control" %>
</div>

  <p>
    <%= form.submit "Salvar", class: "btn btn-success"  %>
  </p>
<% end %>
